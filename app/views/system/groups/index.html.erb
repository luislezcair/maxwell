<%= content_for :header_tabs do %>
  <nav class="nav nav-pills flex-column flex-sm-row">
    <%= link_to t('common.nav.users'), system_users_path, class: 'flex-sm-fill text-sm-center nav-link' %>
    <%= link_to t('common.nav.groups'), system_groups_path, class: 'flex-sm-fill text-sm-center nav-link active' %>
  </nav>
<% end %>

<div class="card">
  <div class="card-header data-header">
    <div class="title"><%= t('.header') %></div>

    <div>
      <%= link_to new_system_group_path, class: 'btn btn-sm btn-primary', data: { toggle: 'tooltip', title: t('.tooltip.new') } do %>
        <i class="fa fa-plus"></i>
      <% end %>
    </div>
  </div>

  <div class="card-body">
    <div id="groups-table-container">
      <%= render 'groups_table' %>
    </div>
  </div>
</div>
