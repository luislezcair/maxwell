document.getElementById('alert-message').innerHTML = '<%= j render 'perform' %>';

document.dispatchEvent(new CustomEvent('billing_exports:perform_disable'));

document.dispatchEvent(
  new CustomEvent('billing_exports:perform_loop',
    {
      detail: {
        url: '<%= job_status_billing_export_path(@billing_export) %>',
      }
    })
);
